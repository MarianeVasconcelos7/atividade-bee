#include <stdbool.h>
#include <math.h>
int check_bigger_quadrado(int a)
{
    int x=0;
    x=pow(a,2);
    return x;
       
}
int check_bigger_cubo(int b){
    int y=0;
    y=pow(b,3);
    return y;
}